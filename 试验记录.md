# 实验记录
后续需要在这里进行相关记录多的更新

首先去掉不管是编码器或者解码器的norm层都会造成训练的不稳定，这说明当网络变大时
要么是这种多尺度结构上就不能没有归一化层
要么时网络变大时就不能没有归一化层
一晚上四次实验都不行卧槽！

cd /home/code/radioMap/model_train
python3 main.py

新版注意力已经最优
老版注意力也已最优
开始有序微调流匹配模型

以下是启动 TensorBoard 的常见命令行命令：

## 基本启动命令

```bash
# 最基本用法 - 指定日志目录
tensorboard --logdir=logs/

# 指定端口（默认6006）
tensorboard --logdir=logs/ --port=6006

# 绑定到所有网络接口（允许远程访问）
tensorboard --logdir=logs/ --host=0.0.0.0

# 指定端口和主机
tensorboard --logdir=logs/ --port=8080 --host=0.0.0.0
```

## 指定不同类型的日志目录

```bash
# 单个目录
tensorboard --logdir=./runs

# 多个目录（用于比较不同实验）
tensorboard --logdir=run1:./experiments/run1,run2:./experiments/run2

# TensorFlow 1.x 风格的日志目录
tensorboard --logdir=./train

# 从多个实验目录加载
tensorboard --logdir=./model1,./model2
```

## 高级选项

```bash
# 设置重新加载间隔（秒）
tensorboard --logdir=logs/ --reload_interval=5

# 限制加载的数据点数量
tensorboard --logdir=logs/ --samples_per_plugin=scalars=1000

# 指定配置文件
tensorboard --logdir=logs/ --window_title="My Experiment"

# 在浏览器中自动打开
tensorboard --logdir=logs/ --open_browser=true
```

## 实际使用示例

```bash
# 从当前目录的 logs 文件夹启动
tensorboard --logdir=./logs --port=6006

# 从多个实验启动，便于比较
tensorboard --logdir=baseline:./logs/baseline,improved:./logs/improved

# 完整的生产环境用法
tensorboard --logdir=./experiments \
            --port=8080 \
            --host=0.0.0.0 \
            --reload_interval=10 \
            --samples_per_plugin=scalars=1000
```

## 在特定环境中使用

```bash
# 在虚拟环境中使用
/path/to/your/venv/bin/tensorboard --logdir=./logs

# 使用 Python 模块方式启动
python -m tensorboard.main --logdir=./logs

# 在 Docker 容器中启动
docker run -p 6006:6006 -v $(pwd)/logs:/logs tensorflow/tensorboard tensorboard --logdir=/logs --host=0.0.0.0
```
